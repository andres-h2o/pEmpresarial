@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Volumetrico {{ $volumetrico->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/volumetrico') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/volumetrico/' . $volumetrico->id . '/edit') }}" title="Edit Volumetrico"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('volumetrico' . '/' . $volumetrico->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Volumetrico" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $volumetrico->id }}</td>
                                    </tr>
                                    <tr><th> Nombre </th><td> {{ $volumetrico->nombre }} </td></tr><tr><th> A </th><td> {{ $volumetrico->a }} </td></tr><tr><th> B </th><td> {{ $volumetrico->b }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
